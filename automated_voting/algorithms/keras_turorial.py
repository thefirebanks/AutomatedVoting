
def main():
    pass
    # Generate profiles for dataset
    # profiles = profiles.generate_profile_dataset(10, 5, ["Adam", "Bryce", "Chad"])

    # Only get the matrices from the profiles
    # dataset = np.array([profile.rank_matrix for profile in profiles])

    # print(dataset.shape)

if __name__ == "__main__":
    main()
